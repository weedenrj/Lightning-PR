Run `bun run lint` to check for linting issues.

Run `bun run build` to build the project.

Fix any errors or warnings that are reported from either command.

Use `read_lints` to check for linter errors in the workspace after running the commands.
